spring:
  application:
    name: springaws

# Secrets manager
aws:
  secretsmanager:
    prefix: /secrets
    defaultContext: application
    profileSeparator: _
    failFast: true
    name: springaws
    enabled: true